# Active Bugs

Any bug written in this file must be fixed before any commit.

When empty (no bug entries below), commits may proceed after the usual tests and tracker updates.

This file is for **must-fix-before-commit** bugs only — not a permanent backlog.

**Exception:** If a bug is unrelated to the current ticket (discovered elsewhere and out of scope), do not leave it here to block forever. Either fix it in a separate small commit first, or move it out of this file into a ticket under `context/issues/` so the commit gate stays honest.

---

## Open bugs

<!-- List active bugs below. Remove an entry only after it is fixed or moved to a ticket. -->
