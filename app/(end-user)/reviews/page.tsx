import { PlaceholderPage } from "@/components/app-shell/placeholder-page";

export default function ReviewsPage() {
  return (
    <PlaceholderPage
      title="Reviews"
      description="Your ratings only — not a public feed. Sign-in and list content wire up in later tickets."
    />
  );
}
