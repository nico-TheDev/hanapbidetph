import { PlaceholderPage } from "@/components/app-shell/placeholder-page";

export default function ProfilePage() {
  return (
    <PlaceholderPage
      title="Profile"
      description="Signed-in home for your name, sign out, and contributions. Guests see a sign-in prompt in a later ticket."
    />
  );
}
