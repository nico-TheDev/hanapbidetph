import { PlaceholderPage } from "@/components/app-shell/placeholder-page";

export default function AddRestroomPage() {
  return (
    <PlaceholderPage
      title="Add CR"
      description="Search an establishment and add a restroom listing. Auth gate and Places search wire up in later tickets."
    />
  );
}
